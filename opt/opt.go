package opt

// Flags represents the configuration options for the while command
type Flags struct {
	// Currently no flags needed for basic while functionality
	// This can be extended in the future if needed
}
